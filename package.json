{
    "name": "menu-app",
    "version": "2.0.0",
    "private": true,
    "description": "قائمة طعام رقمية - Digital Restaurant Menu",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && firebase deploy"
    },
    "devDependencies": {
        "vite": "^5.4.0"
    }
}
